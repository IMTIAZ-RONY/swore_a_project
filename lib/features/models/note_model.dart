class NoteModel {
  String? title;
  String? description;
  DateTime? createdAt;
  DateTime? updatedAt;
  NoteModel({
    this.title,
    this.description,
    this.createdAt,
    this.updatedAt,

  });
}